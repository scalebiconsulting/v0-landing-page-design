# .env.example
# Ejemplo de variables de entorno para el proyecto v0-landing-page-design
# Copia este archivo a .env (o configura las variables en Vercel) y rellena los valores.

# API key de Resend (necesaria para enviar correos desde el servidor)
# - Guardar la clave secreta proporcionada por Resend.
# - En Vercel: añadir como variable de entorno (no usar el repositorio público).
RESEND_API_KEY=

# Dirección "From" que se usará como remitente en los correos enviados por Resend.
# - Debe pertenecer a un dominio verificado en Resend (ej. samuel@scalebi.ai).
# - Si no está verificado Resend rechazará los envíos con 403.
RESEND_FROM=samuel@scalebi.ai

# Dirección de email que recibirá las notificaciones/alertas (admin)
# - Puedes cambiarla a tu correo personal para pruebas (por ejemplo: victorhernandezvivanco75@gmail.com)
ADMIN_EMAIL=samuel@scalebi.ai

# Notas:
# - No incluyas este archivo con valores reales en un repositorio público.
# - Para producción (Vercel): configura estas variables en el panel de Project Settings -> Environment Variables.
# - Antes de enviar correos desde un dominio, verifica el dominio en Resend (https://resend.com/domains)
#   y añade los registros DNS (DKIM / CNAME / TXT) que Resend proporcione.
# - Si recibes errores 403 con Resend: seguramente el dominio del remitente no está verificado.
